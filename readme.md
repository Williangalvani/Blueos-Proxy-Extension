# Blueos HTTP Proxy Extension

This is a basic proxy that allows you to access websites as if you were in the Vehicle
Useful for acessing devices reachable from the Vehicle, but not from the GCS computer.